# CreatorSync – .gitignore (v2.0.0)
# --------------------------------------
# Excludes unnecessary files to keep repository clean and secure
# Investor-facing sidenotes (❖) highlight business value for seed funding
# --------------------------------------

# Node.js dependencies
# ❖ Excludes node_modules to reduce repo size and improve cloning speed
node_modules/

# Environment variables
# ❖ Protects sensitive data (e.g., API keys, DB credentials), ensuring compliance
.env
.env.local
.env.development
.env.test
.env.production

# Log files
# ❖ Prevents sensitive logs from being committed, enhancing security
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Uploads and recordings
# ❖ Excludes user-generated content to keep repo lightweight
uploads/
recordings/

# Build artifacts
# ❖ Ignores temporary build files for clean deployments
dist/
build/
coverage/

# Editor and IDE files
# ❖ Keeps repo clean by excluding editor-specific configurations
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# OS-specific files
# ❖ Prevents platform-specific artifacts from cluttering the repo
.DS_Store
Thumbs.db

# Temporary files
# ❖ Excludes temp files to maintain a professional repo
*.tmp
*.bak
*.swp

# Dependency lock files (optional, included for flexibility)
# ❖ Comment out if you want to commit lock files for reproducibility
# package-lock.json
# yarn.lock

# Docker and CI/CD files
# ❖ Excludes local Docker artifacts for clean builds
.dockerignore
Dockerfile.local
*.docker

# Testing artifacts
# ❖ Ignores test-related temporary files for streamlined CI/CD
test-results/
junit.xml

# Service worker cache
# ❖ Excludes generated service worker files to avoid conflicts
sw.*
